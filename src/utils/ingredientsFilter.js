// import extractArray from "../utils/extractArray";

// const bun = (cards) => cards.filter((item) => item.type === 'bun');

// const main = (cards) => extractArray(cards.filter((item) => item.type === 'main'));

// const sauce = (cards) => extractArray(cards.filter((item) => item.type === 'sauce'));

// export {
//   bun,
//   main,
//   sauce
// }
